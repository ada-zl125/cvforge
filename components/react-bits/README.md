# React Bits Components

This folder is reserved for React Bits components copied into the app.

React Bits uses the shadcn registry configured in `components.json`.

```bash
npx shadcn@latest add @react-bits/<ComponentName>
```

Prefer TypeScript and Tailwind variants. Keep imported effects subtle enough for the document editor.
