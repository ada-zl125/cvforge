# React Bits Components

This folder is reserved for React Bits components copied into the app.

React Bits is consumed through the shadcn registry configured in `components.json`:

```bash
npx shadcn@latest add @react-bits/<ComponentName>
```

Prefer TypeScript + Tailwind variants for this project, and keep any imported effect subtle enough for the resume editor experience.
